
    
    <div class="container">

        <div class="jumbotron">

            <div class="row">
                <h1 class="display-3 text-center">Eat-Da-Burger</h1>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <p class="lead">List a delicious burger you would like to consume!</p>
                </div>

                <div class="col-md-6">
                    <img src="https://cdn.vox-cdn.com/thumbor/3lta23tKnKj-RqlFER_YSKl3VY4=/0x87:500x462/1200x900/filters:focal(0x87:500x462)/cdn.vox-cdn.com/uploads/chorus_image/image/53382189/1512822_793690010647943_107488435_n.0.0.jpg" alt="burger" height="200" width="200">
                </div>
            </div>
        </div>

          {{!-- <form action="/Burgers" method="POST">
            <div class="input-group input-group-lg">
                <input type="text" class="form-control" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-lg">
            </div>
            <input class="btn btn-primary btn-lg" type="submit" value="Submit">
          </form> 

             <div class="row">
      <div class="col s6">
        {{#each Burgers}}
          {{#unless this.devoured}}
            <div>
              <form action="/Burgers/{{this.id}}?_method=PUT" method="POST">
                <button type="submit" class='burgerAvailable'> {{this.burger_name}}</button>
              </form>
            </div>
          {{/unless}}
        {{/each}}
      </div>

      <div class="col s6">
        {{#each Burgers}}
          {{#if this.devoured}}
            <div class='burgerEaten'>
              <div>
                {{this.burger_name}}
              </div>
            </div>
          {{/if}}
        {{/each}}
      </div>
    </div>

     <div class="row">
      <div class="col s6">
        {{#each burger}}
          {{#unless this.devoured}}
            <div>
              <form action="/Burgers/{{this.id}}?_method=PUT" method="POST">
                <button type="submit" class='burgerAvailable'> {{this.burger_name}}</button>
              </form>
            </div>
          {{/unless}}
        {{/each}}
      </div>

      <div class="col s6">
        {{#each burger}}
          {{#if this.devoured}}
            <div class='burgerEaten'>
              <div>
                {{this.burger_name}}
              </div>
            </div>
          {{/if}}
        {{/each}}
      </div>
    </div>
    
    </div> --}}


{{!-- 
<h1>Burgers!</h1>

<h2>Uneaten Burgers</h2>

<ul>
  {{#each Burgers}}
    {{#unless eaten}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cats that are sleepy!</h2>

<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Cat Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add Burger</button>
</form> --}}

{{!-- <h1>Eat Da Burger, Eat it Now!</h1>
<br> --}}

<div id='add'>
    <h2>Add a Burger: </h2>

    <form class="create-update-form" action="/api/burgers" method="POST">
        <div class="form-group">
            <label for="dab">Burger Name:</label>
            <input type="text" id="dab" name="burger_name">
        </div>
        <button class="btn btn-primary" type="submit">Add Burger</button>
    </form>
</div>

<div id='avail'>
    <h2>Burgers Available to Eat:</h2>
    <ul>
        {{#each burgers}} {{#unless this.devoured}}
        <li>
            {{this.id}}. {{this.burger_name}}

            <form action="/api/burgers/:id?_method=PUT" method="POST">
                <input type="hidden" name="devoured" value="false">
                <button class="btn btn-warning" type="submit">Eat Me!</button>
            </form>

        </li>
        {{/unless}} {{/each}}
    </ul>
</div>
<div id='devoured'>
    <h2>Burgers That Have <br>Been Devoured: </h2>

    <ul>
        {{#each burgers}} {{#if this.devoured}}
        <li>
            {{this.id}}. {{this.burger_name}}
        </li>
        {{/if}} {{/each}}
    </ul>
</div>

